export type SiteStatus = "healthy" | "docker_down" | "dead_config" | "drifted" | "disabled" | "error";
export interface DashboardSite { slug: string; projectId: string; customerId?: string | null; customerName?: string | null; domain: string; status: SiteStatus; dockerState?: string | null; lastNginxError?: string | null; lastDockerError?: string | null; configVersion: number; available: boolean; enabled: boolean; upstreamHost?: string | null; upstreamMode?: string | null; upstreamContainerName?: string | null; upstreamExplicitPort?: number | null; tlsMode?: string | null; gateEnabled?: boolean; }
export interface SiteDetail { site: DashboardSite; generatedConfig?: string | null; currentConfig?: string | null; certificateInstalled?: boolean | null; certificateDaysRemaining?: number | null; backups: string[]; }
export interface DashboardCustomerProject { id: string; slug: string; name: string; domain: string; amountDue?: number | null; totalPaid: number; balance: number; status: string; }
export interface DashboardCustomer { id: string; name: string; contactEmail?: string | null; contactPhone?: string | null; billingStatus: string; siteCount: number; health: Record<string, number>; totalBilled: number; totalPaid: number; balance: number; projects?: DashboardCustomerProject[]; }
export interface DashboardCustomerTransaction { id: string; projectId: string; projectName: string; projectSlug: string; amount: number; status: string; gatewayStatus: string; provider: string; providerReference: string; paidAt?: string | null; createdAt: string; }
